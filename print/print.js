/**
 * Outputs a week of the schedule table cells
 * @param  {array} events Contains five events' dates and titles
 * @return {string}       HTML of five table cells
 */
function makeWeek(events) {
	var html = "";
	for (var i=0; i < events.length; i++) {
		
	}
}